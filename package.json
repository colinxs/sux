{
  "name": "kagi-mcp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:sux": "wrangler deploy --config sux/wrangler.jsonc",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run && vitest run --config sux/vitest.config.ts",
    "test:core": "vitest run",
    "test:sux": "vitest run --config sux/vitest.config.ts",
    "test:watch": "vitest",
    "docs": "node sux/scripts/gen-docs.mjs",
    "gen:index": "node sux/scripts/gen-index.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "fflate": "^0.8.3",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.107.0"
  }
}
