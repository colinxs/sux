# sux — function reference

> Auto-generated by `npm run docs` from `sux/src/fns/*.ts`. Do not edit by hand.

**101 functions** · 101 working · 0 planned · 99 with tests.
Each function is one `Fn` file, projected into the MCP `tools/list` by `src/registry.ts`.
Status: ✅ working · 🟡 planned (needs a binding/WASM not yet wired).

### Net / transport (6)

| function | status | test | summary |
|---|---|---|---|
| `scrape` | ✅ | ✓ | Fetch a web page through the residential proxy (falls back to direct) and return its raw content |
| `proxy` | ✅ | ✓ | Raw HTTP transport through the residential exit (direct fallback) |
| `geo_fetch` | ✅ | ✓ | Fetch a URL through the residential proxy, hinting a chosen exit locale |
| `redirects` | ✅ | ✓ | Trace a URL's redirect chain hop by hop |
| `robots` | ✅ | ✓ | Fetch and parse a site's robots.txt |
| `crawl` | ✅ | ✓ | Breadth-first crawl from a seed URL |

### Extract / parse (10)

| function | status | test | summary |
|---|---|---|---|
| `extract` | ✅ | ✓ | Extract structure from HTML (or a URL) |
| `readability` | ✅ | ✓ | Extract the main article content from an HTML page (or url), dropping nav/header/footer/aside/forms/… |
| `grep` | ✅ | ✓ | Regex search over text, line by line |
| `select` | ✅ | ✓ | Query HTML with a CSS selector and return matches |
| `metadata` | ✅ | ✓ | Extract page metadata into a flat JSON object: title, description, keywords, author, canonical, favi… |
| `tables` | ✅ | ✓ | Extract HTML tables into structured data |
| `feed` | ✅ | ✓ | Parse an RSS or Atom feed into normalized items { title, link, published, summary } |
| `sitemap` | ✅ | ✓ | Fetch and parse an XML sitemap |
| `contacts` | ✅ | ✓ | Extract contact info — email addresses, phone numbers (US and E.164-style), and social profiles (twi… |
| `subtitles` | ✅ | ✓ | Convert between SRT and WebVTT subtitles |

### Convert (9)

| function | status | test | summary |
|---|---|---|---|
| `image_convert` | ✅ | ✓ | Transform an image: convert format (png, jpeg, webp, avif), resize, and apply light adjustments via … |
| `csv` | ✅ | ✓ | Convert a JSON array of objects to CSV |
| `fillable` | ✅ | ✓ | Make a PDF fillable by adding interactive AcroForm fields |
| `html` | ✅ | ✓ | Convert Markdown to HTML (common subset: headings, links, bold/em, lists, inline code, fenced code b… |
| `json` | ✅ | ✓ | Convert a document to JSON, dispatching on the source format |
| `markdown` | ✅ | ✓ | Convert HTML to Markdown (common subset: headings h1-h6, links, bold/em, ordered/unordered lists, in… |
| `pdf` | ✅ | ✓ | Best-effort 'anything to PDF' |
| `xml` | ✅ | ✓ | Convert JSON to XML |
| `yaml` | ✅ | ✓ | Convert JSON to YAML (a practical common subset: scalars, nested maps, block sequences) |

### Compress / encode / data (4)

| function | status | test | summary |
|---|---|---|---|
| `compress` | ✅ | ✓ | Compress or decompress data losslessly at the highest ratio |
| `archive` | ✅ | ✓ | Pack files into an archive or unpack one, using fflate (pure JS) |
| `encode` | ✅ | ✓ | Encode or decode text |
| `hash` | ✅ | ✓ | Compute a cryptographic hash of text |

### Token optimization (2)

| function | status | test | summary |
|---|---|---|---|
| `pack` | ✅ | ✓ | Re-encode a JSON array of objects into a compact, token-cheap tabular form so keys aren't repeated p… |
| `declutter` | ✅ | ✓ | Clean HTML uBlock-style before further processing: removes scripts, styles, iframes, ad/consent/news… |

### Text / AI (6)

| function | status | test | summary |
|---|---|---|---|
| `summarize` | ✅ | ✓ | Summarize text or a web page |
| `entities` | ✅ | ✓ | Lightweight entity extraction (regex NER, no model/network) |
| `redact` | ✅ | ✓ | Redact PII from text, replacing each match with [REDACTED:type] |
| `translate` | ✅ | ✓ | Translate text with Workers AI (m2m100) |
| `classify` | ✅ | ✓ | Zero-shot classify text into one of the provided `labels` using Workers AI |
| `ocr` | ✅ | ✓ | Extract text from an image with Workers AI vision |

### Batching / composition (3)

| function | status | test | summary |
|---|---|---|---|
| `batch` | ✅ | ✓ | Map-reduce one sux tool over many inputs |
| `batch_fetch` | ✅ | ✓ | Fetch many URLs concurrently via the residential proxy (direct fallback) |
| `pipe` | ✅ | ✓ | Compose sux tools into a pipeline: each step's text output feeds the next |

### Storage (KV) (5)

| function | status | test | summary |
|---|---|---|---|
| `kv_get` | ✅ | ✓ | Read a value from the KV store by key |
| `kv_put` | ✅ | ✓ | Write a value to the KV store |
| `kv_list` | ✅ | ✓ | List keys in the KV store |
| `kv_delete` | ✅ | ✓ | Delete a key from the KV store |
| `store` | ✅ | ✓ | Store and retrieve arbitrary content in sux's R2 |

### Feedback / meta (1)

| function | status | test | summary |
|---|---|---|---|
| `issue` | ✅ | ✓ | Log a bug/issue with the sux server to its server-side feedback log (KV) |

### Query / APIs (4)

| function | status | test | summary |
|---|---|---|---|
| `search` | ✅ | ✓ | Web search via Kagi |
| `wayback` | ✅ | ✓ | Internet Archive lookups |
| `shop` | ✅ | ✓ | Product search — a router over the dedicated retail fns (no Google Shopping / SerpAPI; that path is … |
| `web_search` | ✅ | ✓ | Web search over Kagi, native Google, and Brave |

### Mail (JMAP) (1)

| function | status | test | summary |
|---|---|---|---|
| `jmap` | ✅ | ✓ | Full Fastmail/JMAP protocol via one generic verb (RFC 8620/8621 + Fastmail MaskedEmail) |

### Memory / recall (1)

| function | status | test | summary |
|---|---|---|---|
| `recall` | ✅ | ✓ | Personal cross-store recall — 'what do I know about X?' answered from YOUR life |

### Other (49)

| function | status | test | summary |
|---|---|---|---|
| `ace` | ✅ | ✓ | Ace Hardware product search via a rendered browser (Ace has no public product API, runs on Kibo/Mozu… |
| `amazon` | ✅ | ✓ | Amazon product search / product-detail via a rendered browser (Amazon has no usable free API and wal… |
| `arxiv` | ✅ | ✓ | Search arXiv (keyless, free) — physics, math, CS, quantitative biology, and more preprints |
| `bestbuy` | ✅ | ✓ | Best Buy Products API (official, free) — product search and detail across Best Buy's catalog |
| `citation` | ✅ | ✓ | Reference management over a vault References/ folder |
| `clinical_trials` | ✅ | ✓ | Search ClinicalTrials.gov (keyless, free) — the NIH registry of clinical studies worldwide |
| `coingecko` | ✅ | ✓ | CoinGecko (keyless, free) — crypto prices and coin search |
| `controld` | ✅ | ✓ | ControlD API (official) — read-only view of your ControlD DNS setup |
| `costco` | ✅ | ✓ | Costco product search |
| `crossref` | ✅ | ✓ | Search CrossRef Works (keyless, free) — scholarly DOI metadata across every discipline |
| `dropbox` | ✅ | ✓ | Dropbox app-folder blob store — the human-facing twin of R2 `store`: files land in /Apps/<app>/ and … |
| `ebay` | ✅ | ✓ | eBay Browse API (official) — search eBay's live marketplace listings by keyword |
| `facebook` | ✅ | ✓ | Facebook Graph API (official) |
| `find_similar` | ✅ | ✓ | Exa neural 'more like this' |
| `fn` | ✅ | — | Escape hatch — call ANY sux leaf tool by name |
| `fontcase` | ✅ | ✓ | Convert text between programming cases and unicode font styles |
| `homedepot` | ✅ | ✓ | Home Depot product search via a rendered browser (Home Depot runs an active Akamai `_abck` JS challe… |
| `ingest` | ✅ | ✓ | Capture into the Obsidian vault (git-backed = the undo; the KV cache is warmed) |
| `kroger` | ✅ | ✓ | Kroger Public API (official, free) — product search, prices, and store locations for Kroger and its … |
| `learn` | ✅ | ✓ | Stateless-learning substrate — teach sux by example, then classify by nearest labeled example (kNN o… |
| `linkedin` | ✅ | ✓ | LinkedIn public profile/company data by scraping (the old Proxycurl API shut down July 2025) |
| `lowes` | ✅ | ✓ | Lowe's product lookup via a rendered browser (Lowe's has no public product API and serves a client-s… |
| `mail_triage` | ✅ | — | Autonomous mail triage: classify inbox messages and (when armed) file them with REVERSIBLE moves onl… |
| `obsidian` | ✅ | ✓ | Work with Obsidian markdown notes |
| `openalex` | ✅ | ✓ | Search OpenAlex (keyless, free) — a 250M+ work open scholarly graph spanning every discipline, with … |
| `oracle` | ✅ | ✓ | A learn-then-answer knowledge oracle backed by KV + Workers AI |
| `people` | ✅ | ✓ | Search public people & organization directories |
| `people_finder` | ✅ | ✓ | Public-source person aggregator — find publicly-listed information about a person by fanning out acr… |
| `places` | ✅ | ✓ | Google Places API (official, free credit) — local business and point-of-interest search from a free-… |
| `preferences` | ✅ | ✓ | A KV-backed style-preference profile that LEARNS over time and continually self-distills — the learn… |
| `product_search` | ✅ | ✓ | Multi-retailer product search — fan one `term` across the retailer fns (kroger, walmart, homedepot, … |
| `pubmed` | ✅ | ✓ | Search PubMed / NCBI (keyless, free) — biomedical and life-sciences literature |
| `put` | ✅ | ✓ | Bulk DOWNLOAD-and-shelve: fetch many URLs concurrently via the residential proxy and content-address… |
| `reddit` | ✅ | ✓ | Reddit read-only API — search posts, browse a subreddit, read a post's comments, or look up a user |
| `render` | ✅ | ✓ | Scrape a JavaScript-rendered page via headless Chromium (Cloudflare Browser Rendering) |
| `selftest` | ✅ | ✓ | Probe the fetch ladder and report which rungs are up |
| `semantic_scholar` | ✅ | ✓ | Search Semantic Scholar (keyless, free) — the Academic Graph across every field of research |
| `stackexchange` | ✅ | ✓ | Search Stack Exchange (keyless, free) — questions across any network site (default stackoverflow; al… |
| `suggest` | ✅ | ✓ | Log a feature/capability request for the sux server to its server-side feedback log (KV) |
| `sux` | ✅ | ✓ | sux capability map — the single, mobile-safe entry point that describes the whole toolset |
| `tailscale` | ✅ | ✓ | Tailscale API (official) — read your tailnet's control plane |
| `tavily` | ✅ | ✓ | Tavily (LLM-oriented web search, free 1k/mo) — returns a synthesized `answer` plus ranked results fo… |
| `todoist` | ✅ | ✓ | Todoist as a BATCH/PIPELINE primitive — bulk array-in operations for use inside pipe/batch, not 50 i… |
| `voice` | ✅ | ✓ | AI text-restyler |
| `walmart` | ✅ | ✓ | Walmart product search and detail via a rendered browser (Walmart runs an active PerimeterX JS chall… |
| `watch` | ✅ | ✓ | Detect whether a page's content changed since the last check |
| `weekly_ad` | ✅ | ✓ | Grocery weekly-ad deals via Flipp (keyless, free) — searches a ZIP's live flyer items by `term` acro… |
| `winco` | ✅ | ✓ | WinCo Foods store locator via a rendered browser (wincofoods.com 403s plain/datacenter fetches) |
| `youtube` | ✅ | ✓ | YouTube Data API v3 (official, free-quota) — video search by `term`, optionally enriched with view/l… |

---

_Legend_: every function returns MCP text content; binary I/O is base64 inside JSON.
Cacheable functions are memoized in KV by a hash of their arguments.
