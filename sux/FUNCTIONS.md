# sux — function reference

> Auto-generated by `npm run docs` from `sux/src/fns/*.ts`. Do not edit by hand.

**110 functions** · 106 working · 4 planned · 106 with tests.
Each function is one `Fn` file, projected into the MCP `tools/list` by `src/registry.ts`.
Status: ✅ working · 🟡 planned (needs a binding/WASM not yet wired).

### Net / transport (13)

| function | status | test | summary |
|---|---|---|---|
| `scrape` | ✅ | ✓ | Fetch a web page through the residential proxy (falls back to direct) and return its raw content |
| `protocol` | ✅ | ✓ | Make an HTTP request through the residential proxy (falls back to direct) |
| `proxy` | ✅ | ✓ | Raw HTTP transport through the residential exit (direct fallback) |
| `geo_fetch` | ✅ | ✓ | Fetch a URL through the residential proxy, hinting a chosen exit locale |
| `latency` | ✅ | ✓ | Measure round-trip latency to a URL by timing HEAD requests through the residential proxy |
| `dns` | ✅ | ✓ | Resolve DNS records via DoH |
| `headers` | ✅ | ✓ | Probe a URL with HEAD: returns HTTP status, response headers, and round-trip latency (ms) |
| `redirects` | ✅ | ✓ | Trace a URL's redirect chain hop by hop |
| `robots` | ✅ | ✓ | Fetch and parse a site's robots.txt |
| `whois` | ✅ | ✓ | Domain registration info via RDAP (JSON WHOIS) |
| `ip_geo` | ✅ | ✓ | Geolocate an IP address (or hostname) |
| `tls_info` | ✅ | ✓ | TLS certificate info for a host via Certificate Transparency logs (crt.sh) |
| `crawl` | ✅ | ✓ | Breadth-first crawl from a seed URL |

### Extract / parse (10)

| function | status | test | summary |
|---|---|---|---|
| `extract` | ✅ | ✓ | Extract structure from HTML (or a URL) |
| `readability` | ✅ | ✓ | Extract the main article content from an HTML page (or url), dropping nav/header/footer/aside/forms/… |
| `grep` | ✅ | ✓ | Regex search over text, line by line |
| `select` | ✅ | ✓ | Query HTML with a CSS selector and return matches |
| `metadata` | ✅ | ✓ | Extract page metadata into a flat JSON object: title, description, keywords, author, canonical, favi… |
| `tables` | ✅ | ✓ | Extract HTML tables into structured data |
| `feed` | ✅ | ✓ | Parse an RSS or Atom feed into normalized items { title, link, published, summary } |
| `sitemap` | ✅ | ✓ | Fetch and parse an XML sitemap |
| `gtin` | ✅ | ✓ | Find and validate product barcodes (GTIN-8/12/13/14, i.e |
| `contacts` | ✅ | ✓ | Extract contact info — email addresses, phone numbers (US and E.164-style), and social profiles (twi… |

### Convert (6)

| function | status | test | summary |
|---|---|---|---|
| `subtitles` | ✅ | ✓ | Convert between SRT and WebVTT subtitles |
| `html_to_pdf` | 🟡 planned | — | Render HTML or a URL to a PDF (base64) |
| `pdf_to_text` | 🟡 planned | — | Extract text (and optionally page layout) from a PDF |
| `pdf_to_images` | 🟡 planned | — | Rasterize PDF pages to images (base64 PNG/JPEG) |
| `office_to_pdf` | 🟡 planned | — | Convert an Office document (docx/xlsx/pptx) to PDF (base64) |
| `image_convert` | ✅ | ✓ | Transform an image: convert format (png, jpeg, webp, avif), resize, and apply light adjustments via … |

### Compress / encode / data (15)

| function | status | test | summary |
|---|---|---|---|
| `shrink` | ✅ | ✓ | Reduce size |
| `optimize` | ✅ | ✓ | Losslessly minify a text asset |
| `json_query` | ✅ | ✓ | Query a JSON document with a dotted path (jq-lite, no dependencies) |
| `compress` | ✅ | ✓ | Compress or decompress data losslessly at the highest ratio |
| `archive` | ✅ | ✓ | Pack files into an archive or unpack one, using fflate (pure JS) |
| `encode` | ✅ | ✓ | Encode or decode text |
| `hash` | ✅ | ✓ | Compute a cryptographic hash of text |
| `diff` | ✅ | ✓ | Line-level diff of two texts via LCS |
| `dedupe` | ✅ | ✓ | Deduplicate data while preserving order |
| `jwt` | ✅ | ✓ | Decode a JWT (and optionally verify it) |
| `flatten` | ✅ | ✓ | Flatten or unflatten nested JSON |
| `sample` | ✅ | ✓ | Take a random sample of `n` items |
| `base_convert` | ✅ | ✓ | Convert an integer between numeric bases |
| `checksum` | ✅ | ✓ | Compute a non-cryptographic checksum of UTF-8 text |
| `qr` | ✅ | ✓ | Encode text into a QR code PNG (base64) or decode a QR image back to text |

### Token optimization (6)

| function | status | test | summary |
|---|---|---|---|
| `pack` | ✅ | ✓ | Re-encode a JSON array of objects into a compact, token-cheap tabular form so keys aren't repeated p… |
| `truncate` | ✅ | ✓ | Truncate text to a budget |
| `count_tokens` | ✅ | ✓ | Estimate the LLM token count of text with a fast heuristic (blend of chars/4 and word count) — an es… |
| `slugify` | ✅ | ✓ | Make a URL/file-safe slug: strips accents (NFKD), lowercases, replaces non-alphanumeric runs with `s… |
| `case_convert` | ✅ | ✓ | Convert identifier casing |
| `humanize` | ✅ | ✓ | Human-friendly formatting of a number |

### Text / AI (6)

| function | status | test | summary |
|---|---|---|---|
| `summarize` | ✅ | ✓ | Summarize text or a web page with Workers AI |
| `entities` | ✅ | ✓ | Lightweight entity extraction (regex NER, no model/network) |
| `translate` | ✅ | ✓ | Translate text with Workers AI (m2m100) |
| `classify` | ✅ | ✓ | Zero-shot classify text into one of the provided `labels` using Workers AI |
| `embed` | ✅ | ✓ | Embed text into vectors with Workers AI (bge-base-en-v1.5, 768-dim) |
| `ocr` | ✅ | ✓ | Extract text from an image with Workers AI vision |

### Privacy (5)

| function | status | test | summary |
|---|---|---|---|
| `redact` | ✅ | ✓ | Redact PII from text, replacing each match with [REDACTED:type] |
| `mask` | ✅ | ✓ | Mask a sensitive string, revealing only its ends (e.g |
| `anonymize` | ✅ | ✓ | Consistently pseudonymize identifiers in text: each unique value becomes a stable token like <email_… |
| `scrub_headers` | ✅ | ✓ | Strip tracking, fingerprinting, and auth headers from a headers object: removes cookie/set-cookie, a… |
| `strip_metadata` | ✅ | ✓ | Remove privacy-sensitive metadata (Exif/GPS, XMP, comments, timestamps) from an image without touchi… |

### Batching (2)

| function | status | test | summary |
|---|---|---|---|
| `batch` | ✅ | ✓ | Broadcast one sux tool over many argument sets |
| `batch_fetch` | ✅ | ✓ | Fetch many URLs concurrently via the residential proxy (direct fallback) |

### Utilities (16)

| function | status | test | summary |
|---|---|---|---|
| `calc` | ✅ | ✓ | Safe arithmetic evaluator (no eval) |
| `units` | ✅ | ✓ | Convert a value between units of the same dimension: length, mass, volume, temperature (C/F/K), data… |
| `datetime` | ✅ | ✓ | Parse, format and shift a datetime (UTC) |
| `validate` | ✅ | ✓ | Validate a string value against a named format or a minimal JSON shape |
| `word_count` | ✅ | ✓ | Text statistics for plain text |
| `sort` | ✅ | ✓ | Sort text lines or a JSON array |
| `frequency` | ✅ | ✓ | Frequency counts over plain text |
| `template` | ✅ | ✓ | Fill a string template with {{var}} placeholders from a `vars` object (dotted keys like {{a.b}} reso… |
| `regex_replace` | ✅ | ✓ | Regex find/replace over text |
| `htmlentities` | ✅ | ✓ | Encode or decode HTML entities |
| `color_convert` | ✅ | ✓ | Convert a color between formats |
| `querystring` | ✅ | ✓ | Parse or build a URL query string |
| `url_parse` | ✅ | ✓ | Parse a URL into its parts using the URL standard |
| `uuid` | ✅ | ✓ | Generate identifiers |
| `random` | ✅ | ✓ | Cryptographically-random values |
| `lint` | ✅ | ✓ | Lint a text document for syntax errors and style issues, reporting each finding with line:col and a … |

### Storage (KV) (4)

| function | status | test | summary |
|---|---|---|---|
| `kv_get` | ✅ | ✓ | Read a value from the KV store by key |
| `kv_put` | ✅ | ✓ | Write a value to the KV store |
| `kv_list` | ✅ | ✓ | List keys in the KV store |
| `kv_delete` | ✅ | ✓ | Delete a key from the KV store |

### Feedback / meta (2)

| function | status | test | summary |
|---|---|---|---|
| `issue` | ✅ | ✓ | Log a bug/issue with the sux server to its server-side feedback log (KV) |
| `suggest` | ✅ | ✓ | Log a proposed improvement to the sux server to its server-side feedback log (KV) |

### Query / APIs (7)

| function | status | test | summary |
|---|---|---|---|
| `search` | ✅ | ✓ | Web search via Kagi (routed through the residential proxy, direct fallback — same smartFetch path as… |
| `pubmed` | ✅ | ✓ | Search PubMed biomedical literature (NCBI E-utilities), distilled to a numbered, citable list — titl… |
| `clinical_trials` | ✅ | ✓ | Search ClinicalTrials.gov, distilled to a numbered list — NCT id, title, status, phase, conditions, … |
| `youtube` | ✅ | ✓ | Fetch a YouTube video's title and transcript via the residential proxy |
| `local_shop` | ✅ | ✓ |  |
| `wayback` | ✅ | ✓ | Internet Archive lookups |
| `barcode_lookup` | ✅ | ✓ | Look up a product by barcode (GTIN/UPC/EAN) via Open Food Facts (free) |

### Other (18)

| function | status | test | summary |
|---|---|---|---|
| `csv` | ✅ | ✓ | Convert a JSON array of objects to CSV |
| `declutter` | ✅ | ✓ | Clean HTML uBlock-style before further processing: removes scripts, styles, iframes, ad/consent/news… |
| `egress` | ✅ | ✓ | Verify that traffic is routing through the Tailscale residential exit |
| `fillable` | ✅ | ✓ | Make a PDF fillable by adding interactive AcroForm fields |
| `grammar` | ✅ | ✓ | Correct grammar, spelling, and punctuation in text without changing its meaning, tone, or style |
| `html` | ✅ | ✓ | Convert Markdown to HTML (common subset: headings, links, bold/em, lists, inline code, fenced code b… |
| `index` | ✅ | ✓ | Build a full-text search index over documents and query it with BM25 ranking |
| `json` | ✅ | ✓ | Convert a document to JSON, dispatching on the source format |
| `markdown` | ✅ | ✓ | Convert HTML to Markdown (common subset: headings h1-h6, links, bold/em, ordered/unordered lists, in… |
| `pdf` | ✅ | ✓ | Best-effort 'anything to PDF' |
| `plaintext` | ✅ | ✓ | Normalize text to clean plaintext |
| `shop` | ✅ | ✓ | Product search across retailers |
| `store` | ✅ | ✓ | Store and retrieve arbitrary content in sux's R2 |
| `tone` | ✅ | ✓ | Rewrite text in a given tone while preserving its meaning |
| `trim` | ✅ | ✓ | Trim or shorten text |
| `web_search` | ✅ | ✓ | Federated web search |
| `xml` | ✅ | ✓ | Convert JSON to XML |
| `yaml` | ✅ | ✓ | Convert JSON to YAML (a practical common subset: scalars, nested maps, block sequences) |

---

_Legend_: every function returns MCP text content; binary I/O is base64 inside JSON.
Cacheable functions are memoized in KV by a hash of their arguments.
